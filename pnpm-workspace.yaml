# better-sqlite3 compiles (or fetches) its native binding; esbuild and
# @parcel/watcher install platform binaries. All three need their build scripts.
allowBuilds:
  '@parcel/watcher': true
  better-sqlite3: true
  esbuild: true
